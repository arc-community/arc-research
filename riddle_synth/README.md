## Installation

```bash
pip install -e ".[dev]"
```
To generate synthetic data, run:
```bash
riddle-synth --config /path/to/config/file --output_dir /path/to/output/dir --show_riddles
```
See the `config` directory for more configuration options.

See all available options:
```bash
riddle-synth --help
```

